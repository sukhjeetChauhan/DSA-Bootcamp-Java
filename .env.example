# --- PostgreSQL Connection Settings ---
# These are used by the 'db' service and the 'app' service.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=postgres

# --- Google API Key ---
# This is required by the 'app' service to use the Gemini model.
GOOGLE_API_KEY="your_google_api_key_here"

# --- Langfuse Observability Settings (Optional) ---
# These are used by the 'app' service if you want to enable Langfuse.
LANGFUSE_PUBLIC_KEY=""
LANGFUSE_SECRET_KEY=""
LANGFUSE_HOST=""
